const dbconfig = {
  host: 'localhost',
  user: 'root',
  password: 'root',
  database: 'chatroom'
};

const websiteconfig = {
  port: 3003,
  mode: 'prod'
}

exports.dbconfig = dbconfig;
exports.websiteconfig = websiteconfig;